#include <bits/stdc++.h>
using namespace std;

string str;
int cnt[26];

int main() {

	cin >> str; // scanf가 안된다.

	for (char a : str) {
		cnt[a - 'a']++;
	}
	
	for (int i=0; i<26; i++) {
		printf("%d ", cnt[i]);
	}
	
	return 0;
}



//중첩for문
#include <bits/stdc++.h>
using namespace std;

int a[9], sum;
vector<int> v;
pair<int, int> ret;

void solve() {
	for (int i = 0; i < 9; i++) {
		for (int j = i + 1; j < 9; j++){
			if (sum - a[i] - a[j] == 100) {
				ret = {i,j};
				return;
			}
		}
	}
}

int main() {

	for (int i = 0; i<9; i++) {
		scanf("%d", &a[i]);
		sum += a[i];
	}
	sort(a, a+9);
	
	solve();

	
	for (int i=0; i<9; i++) {
		if (i == ret.first || i == ret.second) {
			continue;
		}
		printf("%d ", a[i]);
	}
	
	return 0;
}

